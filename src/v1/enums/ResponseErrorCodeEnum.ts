enum ResponseErrorCodeEnum {
  UNKNOWN = 'unknown',
  KAFKA_GROUP_ID_PREFIX_INVALID = 'kafka_group_id_prefix_invalid',
  QUERY_INVALID = 'query_invalid',
  TOPIC_INVALID = 'topic_invalid',
}

export default ResponseErrorCodeEnum
